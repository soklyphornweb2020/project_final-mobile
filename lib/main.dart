import 'package:flutter/material.dart';
import 'package:project_mobile1/ToDoList.dart';

void main() => runApp(Listield());
class Listield extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner:false,
      home: Lists(),
    );
  }
  
}